"""Heddle TUI — terminal dashboard for observing the actor mesh in real time.

Requires the ``tui`` extra::

    uv sync --extra tui

Launch via CLI::

    heddle ui --nats-url nats://localhost:4222
"""
